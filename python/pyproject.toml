[build-system]
requires = ["setuptools>=45", "wheel", "pybind11>=2.6"]
build-backend = "setuptools.build_meta"

[project]
name = "contactipm"
version = "0.1.0"
description = "Python bindings for ContactIPM NMPC solver"
requires-python = ">=3.8"
dependencies = ["pybind11>=2.6", "numpy"]
